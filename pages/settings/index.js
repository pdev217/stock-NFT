import { withLayout } from "../../layout/Layout";
import { Settings } from "../../src/page-components/Settings/Settings";

const SettingsPage = () => <Settings />;

export default withLayout(SettingsPage);
